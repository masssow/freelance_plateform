{% extends 'base-client.html.twig' %}

{% block title %}Hello ClientRegisterController!{% endblock %}

{% block body %}
 
{% block header %}{% endblock %}
{# {% block aside %} {% endblock %}
{% set show_aside = false %}  #}
<div class="container-fluid mt-5">
    
    <div class="row justify-content-center w-100">
        <div class="col-md-4">
            <h3 class="text-center">Inscription du Client</h3>
             {{ form_start(form) }}

            <div class="form-group">
                {{ form_label(form.nomEntreprise) }}
                {{ form_widget(form.nomEntreprise) }}
                {{ form_errors(form.nomEntreprise) }}
            </div>

            {# <div class="form-group">
                {{ form_label(form.adresse) }}
                {{ form_widget(form.adresse, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.adresse) }}
            </div> #}

            <div class="form-group">
                {{ form_label(form.budgetTotal) }}
                {{ form_widget(form.budgetTotal) }}
                {{ form_errors(form.budgetTotal) }}
            </div>

            {# <button type="submit" class="btn btn-primary mt-3">{{ form.submit.vars.label }}</button> #}

            {{ form_end(form) }}
        </div>
    </div>
   
</div>
{% endblock %}
